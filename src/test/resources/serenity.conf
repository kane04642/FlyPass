webdriver {

    driver = chrome

    capabilities {
      browserName = "chrome",
      acceptInsecureCerts = true,

      "goog:chromeOptions" {

        args = [
                "incognito",
                "start-maximized",
                "--use-fake-ui-for-media-stream",
                "--use-fake-device-for-media-stream",
                "no-sandbox",
                "ignore-certificate-errors",
                "disable-default-apps",
                "disable-extensions-file-access-check",
                "disable-infobars",
                "disable-gpu",
                "disable-notifications",
                "disable-popup-blocking",
                #"headless=chrome"
        ]
          prefs {
              handlesAlerts = true,
              common.unexpectedAlertBehaviour = ignore,
              default_content_settings.popups = 0,
              default_content_settings.geolocation = 2,
              credentials_enable_service = false,
              password_manager_enabled = false
          }

        excludeSwitches = ["enable-automation","load-extension"]
      }

      timeouts {
        implicit = 15000
        pageLoad = 300000
      }
    }
    wait.for.timeout = 20000
}
########################################################################################################################
# Configuración de serenity                                                                                            #
########################################################################################################################
serenity {
    # Captura de pantalla
    take.screenshots = AFTER_EACH_STEP
    # Tiempo entre acción
    step.delay = 2000
    report {
       accessibility = true
    }
    project {
        name = "Oficina Virtual"
    }

    console.colors=true
    verbose.steps=true
    console.headings=ascii
    logging=VERBOSE
}

########################################################################################################################
# Configuración del endpoint por ambiente                                                                              #
########################################################################################################################
environments{
    default{
        webdriver.base.url = "https://portaldev.calidda.com.pe:82/oficinavirtual"
    }
    qa{
        webdriver.base.url = "https://portalqa.calidda.com.pe/oficinavirtual"
    }
    dev{
        webdriver.base.url = "https://portaldev.calidda.com.pe:82/oficinavirtual"
    }

    noCliente{
        webdriver.base.url = "https://portaldev.calidda.com.pe:81/frontov/ofivirtual/reclamos-external"
    }
}
## Configuración de ussuario
users {
  cliente {
    username = "a.alguinzones@gmail.com"
    password = "123456"
  }
  # Puedes añadir más actores aquí
  clienteQA {
    username = "a.alguinzones@gmail.com"
    password = "Aa123456789$$"
  }
}
########################################################################################################################
# Configuración reportes JUnit                                                                             #
########################################################################################################################
# Habilitar reportes JUnit
serenity {
  test.root = "com.tuempresa"
  outputDirectory = "target/site/serenity"

  # Configurar para generar reportes JUnit
  junit {
    enabled = true
  }
}

# Configurar el formato de salida para los reports
environments {
  default {
    serenity.outputDirectory = "target/site/serenity"
  }
}